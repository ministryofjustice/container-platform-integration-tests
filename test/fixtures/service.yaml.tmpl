apiVersion: v1
kind: Service
metadata:
  name: {{ .serviceName }}
  namespace: {{ .namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .podName }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080